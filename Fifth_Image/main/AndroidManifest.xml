<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmail.candy7manner.fifth_image">
    <!-- 인터넷 접근하려면 메니페스트 권한(PERMISSION) 획득해야 한다. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 내부 저장소에 접근하려면 권한 (PERMISSION) 획득해야 한다 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Phoneimgae"></activity>
        <activity android:name=".Usevisibility" />
        <activity android:name=".Internetimage" />
        <activity android:name=".Variableimage" />
        <activity android:name=".ChangeImage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>